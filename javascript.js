

function computerPlay () {
//create an array for the computer to choose from
let computerPlay = ["Rock", "Paper", "Scissors"];
//allow the computer to pick a random number between 0-2
let comppick = Math.floor (Math.random ()* 3);




// Computer Selection 

if (computerSelection === playerSelection){
    // display "It's a Tie!"
}

else if (computerSelection = "Rock" && playerSelection = "Paper" ) {
    // display "Paper beats Rock."
    playerScore ++;
    round++;
}


else if (computerSelection = "Paper" && playerSelection = "Rock" ){
    // display "Paper beats Rock."
    computerScore ++;
    round++;
}


else if (computerSelection = "Scissor" && playerSelection = "Rock" ){
    // display "Rock beats Scissor."
    playerScore ++;
    round++;
}
}
else if (computerSelection = "Rock" && playerSelection = "Scissor" ){
    // display "Rock beats Scissor"
    computerScore ++;
    round++;
}
}
else if (computerSelection = "Paper" && playerSelection = "Scissor" ){
    // display " Scissor beats Paper.";
    playerScore ++;
    round++;
}
}
else (computerSelection = "Scissor" && playerSelection = "Paper" ){
    // display " Scissor beats Paper.";
    computerScore ++;
    round++;
}
}


let playerScore = 0;
let computerScore= 0;
let round = 1;

if (round === 5){
    if (computerScore - playerScore = 0)
    //display "It's a Tie!"
}
else if (computerScore - playerScore = negative)
    //display "You win!"

else if (computerScore - playerScore = positive result)
    //display "Computer Wins. Play Again?"
